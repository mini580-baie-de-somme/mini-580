# mini-580.local — nginx reverse proxy to Next.js dev server
# Install: sudo ln -sf /path/to/mini-580/nginx/mini-580.local.conf /etc/nginx/sites-enabled/
# Add to /etc/hosts: 127.0.0.1 mini-580.local

server {
    listen 80;
    server_name mini-580.local;

    client_max_body_size 110M;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
